[pytest]
DJANGO_SETTINGS_MODULE = rh.settings
python_files = test.py test_*.py *_test.py tests.py tests_*.py *_tests.py
addopts =
    --doctest-modules 
    --strict-markers
markers =
    slow: Run tests that are slow.
    fast: Run tests that are fast.